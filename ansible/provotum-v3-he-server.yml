---

- name: Set up all server applications for Provotum 3.0 HE
  hosts: all
  become: yes
  vars:
    - update_apt_cache: yes

  roles:
    - docker
    - docker-compose
    - provotum-v3-he-server-inst
    - role: reverse-proxy
      vars:
        network: files_provotum-server
