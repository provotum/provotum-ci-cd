---

- name: Set up Provotum v2
  hosts: all
  become: yes
  vars:
    - update_apt_cache: yes

  roles:
    - docker
    - docker-compose
    - provotum-v2-server-inst
    - provotum-v2-post-voter
    - role: reverse-proxy
      vars:
        network: e-voting
